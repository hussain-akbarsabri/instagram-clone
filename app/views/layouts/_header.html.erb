<header>
  <div class="container">
    <div class="row">
      <div class="col-md-4 header-logo">
          <%= link_to image_tag("instagram_logo"), root_path %>
      </div>
      <div class="col-md-3 header-search">
        <%= form_with url: search_path, local: true, method: :get, html: { class: "form-inline" } do |form| %>
          <%= form.text_field :q, placeholder: "Search", data: { behaviour: "autocomplete" }, class: "form-control" %>
        <% end %>
      </div>
<<<<<<< HEAD
      <div class="col-md-3 navbar_margin">
        <%= link_to '<i class="glyphicon glyphicon-home"></i>'.html_safe, root_path, class: "btn" %>
        <%= link_to '<i class="glyphicon glyphicon-inbox"></i>'.html_safe, request_path(current_user), class: "btn" %>
        <%= link_to image_tag(current_user.image), user_path(current_user), class: 'header-profile-wrapper' if current_user.image.attached? %>
        <%= link_to '<i class="glyphicon glyphicon-log-out"></i>'.html_safe, destroy_user_session_path, :method => :delete, class: "btn" %>
=======
      <div class="col-md-4 header-search">
        <%= form_with url: search_path, local: true, method: :get, html: { class: "form-inline" } do |form| %>
          <%= form.text_field :q, placeholder: "Search", data: { behaviour: "autocomplete" }, class: "form-control" %>
          <!--<%= form.button "Search", class: "btn btn-outline-success" %>-->
        <% end %>
      </div>
      <div class="col-md-4 navbar_margin">
          <%= link_to '<i class="fas fa-home"></i> Feed'.html_safe, root_path, class: "btn" %>
          <%= link_to "#{current_user.username}", user_path(current_user), class: "btn" %>
          <%= link_to "Requests", request_path(current_user), class: "btn" %>
          <%= link_to 'Logout', destroy_user_session_path, :method => :delete, class: "btn" %>
>>>>>>> autocomplete search
      </div>
    </div>
  </div>
</header>