<table>
  <tr>
    <th>Caption</th>
  </tr>
 
  <% @posts.each do |post| %>
    <tr>
      <td><%= post.caption %></td>
      <td><%= link_to 'Show', user_posts_path(post) %></td>
    </tr>
  <% end %>
</table>