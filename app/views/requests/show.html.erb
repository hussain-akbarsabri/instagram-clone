<table>
  <tr>
    <th>Accounts which have requested</th>
    <th>Accept Request</th>
  </tr>
 
  <% @requests.each do |request| %>
    <tr>
      <td><%= request.follower_id %></td>
      <td><%= link_to 'Accept the request', accept_request_path() %></td>
    </tr>
  <% end %>
</table>
